version: "2.2"
services:
  mysqldb:
    container_name: productdb
    image: mysql/mysql-server:8.0
    restart: unless-stopped
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=raedjd
      - MYSQL_DATABASE=achatdb
      - MYSQL_USER=raed
      - MYSQL_PASSWORD=raedjd
    volumes:
      - achatdb:/var/lib/mysql

  product_app:
    build: ./
    container_name: productspring
    restart: on-failure
    environment:
      - spring.datasource.url=jdbc:mysql://mysqldb:3306/achatdb?createDatabaseIfNotExist=true&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    ports:
      - 8001:8001
    depends_on:
      - mysqldb

volumes:
  achatdb: